!!! XML
%kml{:xmlns => "http://www.opengis.net/kml/2.2"}
  %Document
    %name= "@#{id}'s recent locations" 
    - points.each do |point|
      %Placemark
        %name= "#{point[1]}"
        %Point
          %coordinates= "#{point[0][1]},#{point[0][0]}"